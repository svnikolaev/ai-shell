# ai-shell

**ai-shell** — утилита для генерации bash-команд по запросу на русском языке. Позволяет получать команду и её объяснение, кэширует результаты, проверяет опасные команды по стоп‑листу.

## Возможности

- Генерация команд через LLM (OpenRouter, GPTunneL, локальная Ollama и другие OpenAI‑совместимые бэкенды).
- Кэширование повторяющихся запросов.
- Режим объяснения (`-e`) — команда и описание на русском.
- Повтор последней команды — `ai` без аргументов или `ai -l`.
- Стоп‑лист (настраиваемый) для блокировки опасных команд.
- Диагностика (`--test`) — проверка конфигурации и доступности бэкендов.
- Конфигурация в `~/.config/ai-shell/config.toml`.
- Один бинарник без зависимостей (Rust).

## Установка

### Из исходников

```bash
git clone https://github.com/yourusername/ai-shell.git
cd ai-shell
cargo build --release
sudo cp target/release/ai-shell /usr/local/bin/ai
```

### Через cargo

```bash
cargo install --git https://github.com/yourusername/ai-shell.git
```

После установки проверьте:

```bash
ai --help
```

## Конфигурация

Файл конфигурации: `~/.config/ai-shell/config.toml`.  
Пример с двумя бэкендами (OpenRouter и локальная Ollama):

```toml
# Язык объяснений (ru/en)
explain_language = "ru"

# Стоп-лист
stop_list = [
    "rm -rf /",
    "rm -rf /*",
    "dd if=/dev/zero of=/dev/sd",
    ":(){ :|:& };:",
    "chmod -R 777 /",
    "> /dev/sda",
    "mv /* /dev/null",
    "mkfs",
    "fdisk",
]

# Основной бэкенд — OpenRouter
[[backends]]
api_url = "https://openrouter.ai/api/v1/chat/completions"
api_key = "sk-or-v1-..."          # ваш ключ
model = "nvidia/nemotron-nano-12b-v2-vl:free"
timeout_secs = 30

# Резервный бэкенд — локальная Ollama (должен быть запущен сервер)
[[backends]]
api_url = "http://localhost:11434/v1/chat/completions"
api_key = ""
model = "qwen2.5:7b"
timeout_secs = 10
```

Поле `api_key` можно задать через переменную окружения `AI_API_KEY` (приоритет выше).

### Где взять API-ключи

#### OpenRouter

1. Зарегистрируйтесь на [openrouter.ai](https://openrouter.ai).
2. Перейдите в раздел [Keys](https://openrouter.ai/keys), создайте ключ.
3. Для бесплатных моделей включите опцию **Enable training and logging** в [Privacy Settings](https://openrouter.ai/settings/privacy).

#### GPTunneL (российский сервис, оплата в рублях)

1. Зарегистрируйтесь на [gptunnel.ru](https://gptunnel.ru).
2. Пополните баланс (минимум 50 ₽).
3. В личном кабинете создайте API-ключ.
4. Используйте в конфиге, например:

   ```toml
   api_url = "https://gptunnel.ru/v1/chat/completions"
   model = "gpt-3.5-turbo"   # или другую доступную модель
   ```

   Список моделей доступен на сайте.

## Использование

```bash
# Получить команду
ai "распакуй archive.tar.gz"

# Команда + объяснение
ai -e "найди все файлы больше 100М"

# Последняя команда
ai -l

# Выполнить последнюю команду (осторожно!)
ai -l | sh

# Без аргументов — показать последнюю команду или справку
ai
```

### Флаги

| Флаг     | Назначение                                     |
|----------|------------------------------------------------|
| `-e`     | Показать команду и объяснение                  |
| `-l`     | Вывести последнюю команду                      |
| `-c`     | Игнорировать кэш                               |
| `--test` | Диагностика конфигурации и бэкендов            |
| `-h`     | Справка                                        |

## Диагностика

```bash
ai --test
```

Проверяет:

- наличие и метаданные конфига;
- доступность кэш-директории;
- стоп-лист (если поле есть в файле, но пусто — выдаётся предупреждение);
- каждый бэкенд (тестовый запрос).

## Как это работает

1. Пользователь вводит запрос.
2. Если ответ есть в кэше — возвращается из кэша.
3. Иначе последовательно опрашиваются бэкенды из конфига.
4. Полученная команда проверяется по стоп‑листу.
5. Команда (и объяснение) сохраняется в кэш и в `last_command`.
6. Вывод зависит от флагов.

**Важно:** утилита не выполняет команды автоматически.

## Требования

- Linux (другие Unix подобные ОС не тестировались).
- Для удалённых бэкендов — API-ключ.
- Для локального бэкенда — установленная и запущенная Ollama.

## Разработка и тестирование

### Тесты

```bash
cargo test
```

- юнит-тесты в файлах исходного кода;
- интеграционные тесты в `tests/`.

### Сборка

```bash
cargo build --release
```

Бинарник: `target/release/ai-shell`.

### Зависимости

Основные крейты: `clap`, `ureq`, `serde`, `toml`, `dirs`, `blake3`, `anyhow`.

## Автоматизация сборки (Makefile)

Доступные цели:

- `make test` — запустить тесты.
- `make build` — собрать release.
- `make install` — установить в `~/.cargo/bin/ai-shell`.
- `make link` — создать символическую ссылку с именем `LINK_NAME` (по умолчанию `ai`).
- `make all` (или просто `make`) — test, build, install, link.
- `make clean` — очистить.
- `make uninstall` — удалить бинарник и ссылку.

Переопределить имя ссылки:

```bash
make LINK_NAME=ia        # ссылка ia
make install link LINK_NAME=myai
```

## Лицензия

MIT © 2026
